{
  "name": "mock-cas",
  "version": "0.2.0",
  "private": true,
  "description": "Timed mock exam — H3C CAS Cloud Automation System. Cloudflare Workers Static Assets.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
